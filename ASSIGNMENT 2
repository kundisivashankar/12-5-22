def ams(n):
    num=n
    rev=0
    while n>0:
        rem=n%10
        rev += rem **3
        n//=10
    
    if num == rev:
        return ("amstrong")
    else:
        return ("NOt amstrong")

x=int(input("Enter the number"))
b=ams(x)
print("output is",b)
